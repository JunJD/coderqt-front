import React from 'react';
import './index.less';
const Login = () => {
    // const { login } = useAuth();

    return (
        <div className="login">
            <h1>Login</h1>
        </div>
    );
};

export default Login;
